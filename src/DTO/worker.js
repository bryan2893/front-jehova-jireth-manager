export default class WORKER{
    constructor(workerId,fullName,rol,password){
        this.workerId = workerId;
        this.fullName = fullName;
        this.rol = rol;
        this.password = password;
    };
}